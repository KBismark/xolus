{
  "name": "xolus",
  "version": "2.0.0",
  "description": "The modern, html rendering framework.",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "preversion": "npm run build",
    "version": "npm publish"
  },
  "author": "Bismark Yamoah",
  "license": "MIT",
  "dependencies": {
    "statestorejs": "^1.2.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
